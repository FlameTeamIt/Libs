cmake_minimum_required(VERSION 3.14)

set(NAME Common)
set(LIBRARY_ALIAS_NAME ${FLAME_NAMESPACE}::Common::Headers)
set(DEPENDENCY_LIST
	${FLAME_NAMESPACE}::Common::Macroses::Headers
	${FLAME_NAMESPACE}::Common::OsTypes::Headers
	${FLAME_NAMESPACE}::Common::Traits::Headers
)
set(HEADER_LIST_FILE ${CMAKE_CURRENT_SOURCE_DIR}/Headers.cmake)

header_library(
	LIBRARY_NAME       "${NAME}"
	LIBRARY_ALIAS_NAME "${LIBRARY_ALIAS_NAME}"
	HEADER_LIST_FILE   "${HEADER_LIST_FILE}"
	DEPENDENCY_LIST    "${DEPENDENCY_LIST}"
	INCLUDE_PATH       "${FLAME_INCLUDE_PATH}"

	${FLAME_CMAKE_DEBUG}
)
