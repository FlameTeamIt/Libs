set(NAME Analysers)

set(MODULE_WITH_FILES FALSE)
set(LIST_OF_SUBMODULES Ubjson)
set_object_module(
    ${NAME}
    ${MODULE_WITH_FILES}
    ${LIST_OF_SUBMODULES}
    LIST_OF_MODULES
)

set(${NAME}_LIST_OF_SUBMODULES
    ${LIST_OF_MODULES}
    PARENT_SCOPE
)

add_tests(FALSE)
