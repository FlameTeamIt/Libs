set(NAME Analysers)

set(LOCAL_HEADER_LIST)
actualize_header_list(HEADER_LIST LOCAL_HEADER_LIST)
list(FILTER LOCAL_HEADER_LIST
    INCLUDE
    REGEX "${NAME}"
)
set(HEADER_LIST ${LOCAL_HEADER_LIST})
unset(LOCAL_HEADER_LIST)

list(FILTER HEADER_LIST
    INCLUDE
    REGEX "Analysers"
)

set(MODULE_WITH_FILES FALSE)
set(MODULES Ubjson)
set_object_module(
    ${NAME}
    ${MODULE_WITH_FILES}
    ${MODULES}
    LIST_OF_MODULES
)

set(${NAME}_MODULES
    ${LIST_OF_MODULES}
    PARENT_SCOPE
)

add_tests(FALSE)
