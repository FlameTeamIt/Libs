set(NAME Posix)

set(MODULE_WITH_FILES TRUE)
set(LIST_OF_SUBMODULES FALSE)

set_object_module(
    PosixStreams
    ${MODULE_WITH_FILES}
    ${LIST_OF_SUBMODULES}
    LIST_OF_MODULES)

set(${NAME}_LIST_OF_SUBMODULES ${LIST_OF_MODULES} PARENT_SCOPE)
