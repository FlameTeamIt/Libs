name: 'Build libraries'

on:
  push:
    branches:
      - '**'
    paths:
      - '.github/workflows/**'
      - 'cmake/**'
      - 'include/**'
      - 'src/**'
      - 'tests/**'
      - '**/CMakeLists.txt'
      - '**/*.pro'
  pull_request:
    branches:
      - '**'
    paths:
      - '.github/workflows/**'
      - 'cmake/**'
      - 'include/**'
      - 'src/**'
      - 'tests/**'
      - '**/CMakeLists.txt'
      - '**/*.pro'

jobs:
  build-libraries:
    name: 'Build libraries'

    strategy:
      matrix:
        system:
          - windows-latest
          - ubuntu-latest

    runs-on: ${{ matrix.system }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure project
        run: |
          cmake -S . -B build -G "Ninja" -DFLAME_CONAN_SUPPORT=OFF -DCMAKE_BUILD_TYPE=Release

      - name: Build project
        run: |
          cmake --build build --config Release
